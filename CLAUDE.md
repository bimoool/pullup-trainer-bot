# pullup-trainer-bot

Telegram-бот-тренер по подтягиваниям. Python 3.12, aiogram 3, PostgreSQL 16,
SQLAlchemy 2 (async), Alembic, APScheduler, Redis (FSM-хранилище), gspread,
openpyxl. Long polling, без вебхука. Хостится на VPS `<VPS_HOST>`
(root SSH, `/root/pullup-trainer-bot`, GitHub `bimoool/pullup-trainer-bot`).

**Прежде чем грепать код — построй граф.** `graphifyy` (команда `graphify`)
уже в dev-зависимостях, детерминированный граф вызовов/импортов по AST без
LLM, ~10-20с на весь репозиторий. Подробности и когда использовать — скилл
`codebase-map`. Не открывай это как последний вариант после серии `grep` —
проверяй граф первым: задача часто затрагивает несколько слоёв
(`app/domain/` → `app/db/repositories/` → `app/bot/handlers/`/`app/web/`),
где связи размазаны и совпадают по именам лишь частично.

**VPS общий с другим проектом** (`reeltrack-bot`/`reeltrack-postgres`,
контейнеры `reeltrack-*`, отдельная директория `/root/reeltrack-bot`) —
все `docker compose`-команды выполнять из `/root/pullup-trainer-bot`, без
`-p`/глобальных флагов, которые могли бы задеть чужие контейнеры. Порт `db`
на хосте — эфемерный (`127.0.0.1::5432`, `docker compose port db 5432`
чтобы узнать) именно для того, чтобы не конфликтовать с другими локальными
проектами.

## Архитектура

- **`app/domain/`** — чистая бизнес-логика (прогрессия, аномалии,
  ачивки, правила готовности к тренировке, отчёты). Ни одного импорта
  aiogram или SQLAlchemy — проверяется буквально (`grep -rl
  "aiogram\|sqlalchemy" app/domain/` должен быть пуст). Функции чистые,
  датаклассы `frozen=True`, деньги/вес — `Decimal`, не `float`. Домен
  покрыт unit-тестами в `tests/test_*.py` (корень `tests/`), без Postgres.
- **`app/db/repositories/`** — один репозиторий на агрегат
  (`WorkoutRepository`, `UserRepository`, ...), только они трогают
  SQLAlchemy-сессию и конвертируют ORM ↔ доменные датаклассы
  (`app/domain/session.py: WorkoutRecord/BlockAssignment` и т.п.).
  Интеграционные тесты — `tests/test_repositories/`, на реальном Postgres.
- **`app/services/`** — оркестрация поверх репозиториев: пишет через
  репозиторий, шлёт событие в `EventRepository`, начисляет монеты
  (`GamificationService`), проверяет ачивки. Тесты — `tests/test_services/`,
  реальный Postgres.
- **`app/bot/handlers/`** — тонкий слой aiogram поверх `services`/`domain`.
  Не должен содержать бизнес-логику прогрессии/начислений — только сбор
  ввода, FSM-состояние, вызов сервиса, форматирование ответа. Тесты —
  `tests/test_bot/`, реальный aiogram `Dispatcher.feed_update` со стаб-сессией
  бота (`tests/test_bot/conftest.py`), не моки хендлеров.
- **`app/web/`** — FastAPI Mini App, тонкий слой поверх тех же
  `repositories`/`services`/`domain`, что и бот, не дублирует логику;
  `webapp-frontend/` (React + Vite) собирается в статику и отдаётся тем же
  процессом одним origin'ом. Тесты — `tests/test_web/`. Полная история
  решений и разборов — `docs/mini-app.md`.
- **`app/workers/`** — фоновые задачи через APScheduler (синк Google
  Sheets, опрос Robokassa OpState, еженедельные/дневные рассылки) —
  polling-архитектура, не вебхуки: без домена/HTTPS у бота негде принимать
  входящие колбэки. Если воркеру нужно программно привязать следующее
  сообщение пользователя к событию таймера (не к колбэку — входящего
  `Update` у таймера нет) — `dispatcher.fsm.get_context(bot=bot,
  chat_id=..., user_id=...)` выставляет FSM-состояние напрямую (см.
  `weekly_digest.py`); для этого `register()` воркера должен получить
  `dispatcher`, не только `bot` (`main.py::build_scheduler`). Redis-хранилище
  FSM (не `MemoryStorage`) означает, что такая связка переживает
  рестарт/деплой между отправкой напоминания и ответом на него. Подробности
  по конкретным рассылкам — `docs/notifications.md`.

**Деплой-образ не содержит `.git` и не имеет `git`/`gh`** — `Dockerfile`
копирует только `app/`, `pyproject.toml`, `alembic.ini` (не `COPY . .`).
Проверено буквально: `docker run --rm <image> sh -c "test -d /app/.git"`
— пусто, `which git gh` — пусто. Любая функция, которой нужна история
коммитов или GitHub API не может использовать локальный `git log` или
личный `gh`-доступ разработчика — только собственный GitHub-токен,
положенный в `.env` на сервере и прочитанный через `app.config.settings`.

## Изменение формулы прогрессии или схемы БД — план перед кодом

Для нетривиальных изменений домена прогрессии (шаг роста, пороги, переходы
снаряда) или новых колонок/таблиц — сначала план с конкретными расчётами на
реальных примерах (числа, не абстрактные формулы), явное согласование, и
только потом реализация. Тесты пишутся на **каждый пример из плана**, с
точными ожидаемыми значениями — это почти всегда ловит расхождение между
тем, что казалось очевидным на бумаге, и тем, что реально считает код (см.
«Стиль тестирования» в `docs/testing.md`). Полная история ревизий формулы
прогрессии — `docs/progression.md`.

## Известные классы багов (короткая версия)

Каждый — реальный прод-инцидент с полным разбором в `docs/incidents.md`:

- **`callback.message.from_user` — это бот, не человек.** В любом
  хендлере, где `message` может прийти из `CallbackQuery`, брать
  `telegram_id` только из `callback.from_user.id`, никогда из
  `message.from_user.id`.
- **`message.text` — `None` у сообщения с картинкой.** Любой хендлер,
  принимающий свободный текст от админа для пересылки, должен проверять
  `message.photo` и использовать `message.caption` как текст в этом случае.
- **Альбом (несколько фото) — Telegram шлёт его как несколько `Update`,
  не один** (общий `media_group_id`). Нужна буферизация до хендлера
  (`app/bot/middlewares.py::MediaGroupMiddleware`), не точечная проверка
  `message.photo` внутри самого хендлера.
- **`Input`/`Select` из `@telegram-apps/telegram-ui` без `<Section>` —
  визуально невидимы** (сливаются с фоном страницы), хотя технически
  работают. Всегда оборачивать в `<Section>`/`<List>`.

## Прочие устоявшиеся решения

- **Архивировать, не удалять.** Любой «сброс»/«убрать» исторических
  данных — перенос в `*_archive_v1`/`*_archive_admin_reset`-таблицу, не
  `DELETE`. Перед удалением enum-значения из БД (см. пример с Tribute,
  `8a1f4c6e9b2d`) — сначала вручную проверить, нет ли исторических строк
  с этим значением; если есть — не удалять значение, оставить как
  «исторический, не используется», задокументировав это в комментарии
  рядом с определением enum.
- **Одна причина падения = один хендлер, не общий `try/except` в проде
  молча.** `cd0eefa` завёл глобальный обработчик необработанных
  исключений на уровне диспетчера — он логирует и уведомляет админа
  (`ADMIN_UNHANDLED_EXCEPTION_NOTIFICATION`), не проглатывает молча.
  Если пишешь код, который может упасть предсказуемо — лови и обрабатывай
  сам, не полагайся на этот catch-all как на способ скрыть баг.
  `state.clear()` — до записи в БД в `_finalize_workout`, не после
  (гонка двойного тапа: см. `2b92942`).
- **`app.bot.parsing.MAX_REPS = 999`** — единственный источник верхнего
  предела на число повторений (`parse_reps`/`parse_int_sequence`,
  `onboarding.py`). Не заводить локальных копий лимита в отдельных
  хендлерах (баг, `673bf65`: старый `MAX_REASONABLE_REPS=100` в
  `onboarding.py` отклонял обычные трёхзначные результаты вроде 102).
- **Числовые константы продуктовых решений, не научных фактов** —
  фиксировать явно в докстринге при вводе (пример: потолок 30 повторений,
  откат до 20/30, старт веса 5 кг, шаг 5% в `app/domain/progression.py` —
  докстринг `recalculate_volume_block` явно отделяет, что построено на
  принципах прогрессивной перегрузки, а что — собственный выбор чисел).
- **Смена схемы `workouts`/`blocks`/`workout_sets`/`baselines`/
  `equipment_items`** — проверь, нужно ли то же изменение в соответствующей
  `*_archive_admin_reset` таблице (все пять перечислены в
  `b41882a0e3e8_admin_reset_archive_tables.py`, история инцидента —
  `docs/deploy.md`).

## Куда смотреть дальше

Механика (как что-то запустить/проверить) — в скиллах:
- `codebase-map` — навигация по коду через `graphify`.
- `running-tests` — как прогнать тесты локально, что гоняет CI.
- `deploy-and-verify` — два контура деплоя, ограничения SSH/железа.
- `multi-program` — состояние ветки `feature/multi-program`.
- `product-reference` — что мы строим, референс для UX-решений.

История разборов и продуктовых решений — в `docs/`:
- `docs/mini-app.md` — вся история Mini App (issues #15–#148): auth,
  initData, черновик тренировки, лидерборд, ГТО/WSF, E2E-тестировщик,
  редактирование резин.
- `docs/progression.md` — формулы прогрессии, тест на максимум,
  чередующаяся тяжёлая тренировка, WSF, бэкдейт/удаление записей.
- `docs/incidents.md` — полные разборы известных классов багов выше.
- `docs/payments.md` — `ADMIN_IDS`-обход лимитов, диагностика задержки
  опроса Robokassa.
- `docs/deploy.md` — ручная процедура деплоя, инцидент с архивными
  таблицами.
- `docs/notifications.md` — еженедельный дайджест, push-напоминание о
  тренировке.
- `docs/analytics.md` — бэкфилл колонок Google Sheets.
- `docs/testing.md` — философия «докажи, что тест ловит баг».
- `docs/architecture-multicourse.md` — архитектура многокурсовой платформы.

## Обновление этого файла

Держать живым: после значимой новой фичи или явно установленного нового
соглашения — добавлять/обновлять соответствующую запись в подходящем
`docs/<тема>.md` (не разрастать обратно этот файл), формулируя по факту
кода/истории на момент правки, не по памяти о более раннем состоянии
проекта. Если новая тема не укладывается ни в один существующий
`docs/<тема>.md` — завести новый файл и добавить его в список выше. Если
правка меняет жёсткое правило/архитектурный принцип из разделов выше —
обновить и здесь, не только в `docs/`.
